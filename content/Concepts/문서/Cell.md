---
aliases:
  - 셀
---
# 개요
RNN, LSTM, GRU 등에서, 은닉층에서 활성화 함수를 통해 결과를 내보내는 역할을 하는 노드

한 시점에서의 연산단위 = **시간축 위에서 반복되는 작은 계산 블록**

# 은닉층과의 차이
은닉층은 Layer(공간적) 개념
셀은 시간적 개념.

RNN에서 한 은닉층이 시간축으로 펼쳐지면 여러개의 셀로 보인다.